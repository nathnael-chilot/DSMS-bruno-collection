info:
  name: Enroll (Missing scheduleId)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/enrollments"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "remarks": "Missing scheduleId"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors.
