info:
  name: Get Enrollment By Id (Forbidden)
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/enrollments/{{enrollmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as a DIFFERENT student (not the owner) or unrelated instructor — should return 403.
