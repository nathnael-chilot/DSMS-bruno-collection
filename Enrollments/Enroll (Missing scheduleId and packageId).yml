info:
  name: Enroll (Missing scheduleId and packageId)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/enrollments"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "remarks": "Missing scheduleId and packageId"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors for both scheduleId and packageId.
