info:
  name: Delete Enrollment (Non-Admin, Forbidden)
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/enrollments/{{enrollmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as a student or instructor — should return 403 Forbidden.
