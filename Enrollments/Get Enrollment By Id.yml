info:
  name: Get Enrollment By Id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/enrollments/{{enrollmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
