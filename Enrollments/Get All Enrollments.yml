info:
  name: Get All Enrollments
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/enrollments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Scoped by role: admin sees all, instructor sees their own schedules' enrollments, student sees only their own."
