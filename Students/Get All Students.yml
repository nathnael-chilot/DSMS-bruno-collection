info:
  name: Get All Students
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/students"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
