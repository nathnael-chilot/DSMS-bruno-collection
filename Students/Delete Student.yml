info:
  name: Delete Student
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/students/{{studentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
