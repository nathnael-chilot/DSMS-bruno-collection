info:
  name: Change My Password (Missing Fields)
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/api/profile/change-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "newPassword": "NewPass123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 — currentPassword is required.
