info:
  name: Delete Schedule
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/schedules/{{scheduleId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin-only.
