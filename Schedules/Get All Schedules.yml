info:
  name: Get All Schedules
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/schedules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Any authenticated role (admin, instructor, or student) can view schedules.
