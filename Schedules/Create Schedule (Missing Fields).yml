info:
  name: Create Schedule (Missing Fields)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/schedules"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "remarks": "Missing everything else"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors.
