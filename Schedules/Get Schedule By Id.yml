info:
  name: Get Schedule By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/schedules/{{scheduleId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
