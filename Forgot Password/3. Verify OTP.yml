info:
  name: 3. Verify OTP
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/verify-otp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "{{adminEmail}}",
        "otp": "123456"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const jsonData = res.getBody();
        if (jsonData.resetToken) { bru.setEnvVar('resetToken', jsonData.resetToken); }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Replace the otp value with the 6-digit code received by email. Rejected if expired, even if it would otherwise match. On success, auto-saves resetToken.
