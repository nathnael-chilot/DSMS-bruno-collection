info:
  name: Get All Packages
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/packages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Any authenticated role can view packages.
