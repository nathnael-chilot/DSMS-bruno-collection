info:
  name: Get Package By Id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/packages/{{packageId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
