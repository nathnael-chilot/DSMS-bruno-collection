info:
  name: Create Package (No Token)
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/packages"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Package X",
        "description": "Should fail",
        "price": 10000
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 401 — no token provided.
