info:
  name: Delete Package
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/packages/{{packageId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin-only.
