info:
  name: Create Package (Missing Fields)
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/packages"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Incomplete Package"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors (missing description and price).
