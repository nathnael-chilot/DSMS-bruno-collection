info:
  name: Get Instructor By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/instructors/{{instructorId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
