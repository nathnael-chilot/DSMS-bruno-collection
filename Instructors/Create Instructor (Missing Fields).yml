info:
  name: Create Instructor (Missing Fields)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/instructors"
  body:
    type: multipart-form
    data:
      - name: firstName
        type: text
        value: OnlyFirstName
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors.
