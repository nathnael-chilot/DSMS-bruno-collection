info:
  name: Get All Instructors
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/instructors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
