info:
  name: Monthly Income (Line/Bar Chart)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/monthly-income"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin-only. Paid payment totals grouped by month, last 12 months.
