info:
  name: Payment Status Breakdown (Non-Admin, Forbidden)
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/payment-status-breakdown"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as instructor or student — should return 403. All six analytics endpoints are admin-only.
