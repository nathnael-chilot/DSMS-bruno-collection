info:
  name: Recent Payments (Table)
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/recent-payments?limit=5"
  params:
    - name: limit
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin-only. Most recent payments system-wide. limit is optional, defaults to 10.
