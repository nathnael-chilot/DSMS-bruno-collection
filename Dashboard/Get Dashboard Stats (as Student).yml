info:
  name: Get Dashboard Stats (as Student)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as student. Returns instructorCount and scheduleCount (both system-wide totals).
