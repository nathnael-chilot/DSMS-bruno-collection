info:
  name: Monthly Enrollments (Line/Bar Chart)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/dashboard/monthly-enrollments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin-only. New enrollment counts grouped by month, last 12 months.
