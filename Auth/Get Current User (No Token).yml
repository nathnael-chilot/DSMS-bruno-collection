info:
  name: Get Current User (No Token)
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/auth/me"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 401 Not authorized, no token.
