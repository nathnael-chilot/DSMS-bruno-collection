info:
  name: Get Progress Report By Id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/progress-reports/{{progressReportId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
