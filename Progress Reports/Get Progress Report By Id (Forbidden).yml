info:
  name: Get Progress Report By Id (Forbidden)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/progress-reports/{{progressReportId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as a DIFFERENT student or unrelated instructor — should return 403.
