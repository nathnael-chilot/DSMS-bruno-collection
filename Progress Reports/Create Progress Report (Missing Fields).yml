info:
  name: Create Progress Report (Missing Fields)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/progress-reports"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "remarks": "Missing trainingName, student, and score"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Should return 400 with express-validator field errors.
