info:
  name: Get All Payments
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/payments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Scoped by role: admin sees all payments, student sees only their own."
