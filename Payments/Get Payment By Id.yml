info:
  name: Get Payment By Id
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/payments/{{paymentId}}"
  auth: inherit

runtime:
  variables:
    - name: paymentId
      value: 6a85509696d9bde990a96de1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
