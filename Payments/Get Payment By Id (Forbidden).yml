info:
  name: Get Payment By Id (Forbidden)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/payments/{{paymentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Run while logged in as a DIFFERENT student (not the owner) — should return 403.
